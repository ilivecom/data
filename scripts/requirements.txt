# ETF + Macro Pipeline — Python 依赖
# 安装: pip install -r requirements.txt

# 核心：A 股 ETF 数据
akshare>=1.12.0

# 宏观指标（DXY/US10Y/VIX/NDX/BTC via Yahoo Finance）
yfinance>=0.2.40

# Google Sheets 写入（可选，不用 Sheets 可不装）
gspread>=5.12.0
google-auth>=2.23.0
